import React from "react";
import {withRouter} from "react-router-dom";
import MainView from '../MainView';

class InvestorScreen extends MainView {
    render() {
        return (
            <div>
                <h1>INVESTOR SCREEN</h1>
                <h1>INVESTOR SCREEN</h1>
            </div>
        );
    }
}

export default withRouter(InvestorScreen);
